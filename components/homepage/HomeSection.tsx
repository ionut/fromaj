import Image from "next/image";
import HomepageSlider from "../features/HomepageSlider";

const HomeSection = () => {
  return (
    <section className="hero text-center">
      <HomepageSlider />
    </section>
  );
};

export default HomeSection;
